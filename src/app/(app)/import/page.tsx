import PlaceholderScreen from "@/components/placeholder-screen";

export default function Page() {
  return (
    <PlaceholderScreen
      title="Import / Export"
      sub="Bulk data import for customers, projects, compliance history."
      phase="Phase 5"
      details="The unified import surface; used for real data migration in Phase 9."
    />
  );
}
