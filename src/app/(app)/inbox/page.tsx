import PlaceholderScreen from "@/components/placeholder-screen";

export default function Page() {
  return (
    <PlaceholderScreen
      title="Inbox"
      sub="Personal + shared mailboxes, compose, calls & meetings log."
      phase="Phase 3"
      details="The full email client screens arrive in Phase 3; real Gmail send/receive connects in Phase 7."
    />
  );
}
