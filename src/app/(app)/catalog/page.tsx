import PlaceholderScreen from "@/components/placeholder-screen";

export default function Page() {
  return (
    <PlaceholderScreen
      title="Catalog"
      sub="Parts catalog and price-book import."
      phase="Phase 5"
      details="The catalog from catalog-data.js and the Catalog Import flow."
    />
  );
}
